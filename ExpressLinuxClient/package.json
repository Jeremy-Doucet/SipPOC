{
  "name": "ExpressLinuxClient",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "sequelize": "^6.20.0",
    "sequelize-cli": "^6.4.1"
  },
  "scripts": {
    "start": "node index.js",
    "build": "docker build -t express-linux-client .",
    "runDocker": "docker run --init --rm -p 3001:3001 --name express-linux-client express-linux-client"
  }
}
